# Copy to .dev.vars for local `wrangler pages dev`. In production these are set as
# Cloudflare Pages environment variables so that neither host is ever committed to
# this repository or shipped to the browser.
CRM_ORIGIN=https://example.invalid
CDN_ORIGIN=https://cdn.example.invalid
